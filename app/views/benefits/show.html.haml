    
.page
    = image_tag @benefit.image.url(:medium)
    %h3= @benefit.title
    %p= @benefit.description
    Braught to you by
    %p= @benefit.partner.name
    %h5 Tags
    = render @benefit.categories
    %br
    = link_to 'Redeem!', new_user_benefit_path(current_user, @benefit), class: "btn btn-success btn1"
    = link_to 'Back', benefits_path, class: "btn btn-default"
    - if current_user.try(:admin?)
        
        = link_to 'Edit', edit_benefit_path, class: "btn btn-default"
        = link_to "Delete", benefit_path, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-default"